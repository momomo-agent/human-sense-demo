import SwiftUI

/// Placeholder. FluidAudio integration deferred.
struct DiarizationTestView: View {
    var body: some View {
        VStack {
            Text("Diarization")
                .font(.title)
            Text("Disabled — FluidAudio dep pending")
                .foregroundStyle(.secondary)
        }
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color.black)
    }
}
